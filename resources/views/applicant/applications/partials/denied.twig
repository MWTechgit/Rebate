<div class="card mb-4">
    <div class="card-header">
        <h4 class="mb-0">Application Denied</h4>
    </div> <!-- .card-header -->
    <div class="card-body">
        <p>Sorry but your application has been denied. Below are the reason(s) for the denial.</p>

        <blockquote class="blockquote">
            {% if application.isExpired() %}
                Your rebate application has expired because we didn't receive all the necessary information in a timely manner.
            {% else %}
                {{ application.denial().reason() }}
            {% endif %}
        </blockquote>
    </div> <!-- .card-body -->
</div> <!-- .card -->